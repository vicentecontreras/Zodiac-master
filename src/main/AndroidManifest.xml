<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.raion.zodiac">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectZodiacActivity" />
        <activity android:name=".AcuariusActivity"></activity>
        <activity android:name=".AriesActivity"></activity>
        <activity android:name=".TauroActivity"></activity>
        <activity android:name=".GeminisActivity"></activity>
        <activity android:name=".CancerActivity"></activity>
        <activity android:name=".LeoActivity"></activity>
        <activity android:name=".VirgoActivity"></activity>
        <activity android:name=".LibraActivity"></activity>
        <activity android:name=".EscorpioActivity"></activity>
        <activity android:name=".SagitarioActivity"></activity>
        <activity android:name=".CapricornioActivity"></activity>
        <activity android:name=".PiscisActivity"></activity>
    </application>

</manifest>